<!-- we will render seperate nav bars depending on if the user is signed in or not. -->
<% if !preacher_signed_in? %>
  <%= render '/layouts/nav_guest' %>
<% else %>
  <%= render '/layouts/nav_preacher' %>
<% end %>
